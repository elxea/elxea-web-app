# Shopify Storefront API
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_STOREFRONT_ACCESS_TOKEN=your-storefront-access-token

# Shopify Admin API (required for subscription management & webhooks)
SHOPIFY_ADMIN_ACCESS_TOKEN=your-admin-access-token
SHOPIFY_WEBHOOK_SECRET=your-webhook-secret

# Shopify Customer Account API
SHOPIFY_CUSTOMER_ACCOUNT_CLIENT_ID=your-client-id
SHOPIFY_SHOP_ID=your-shop-id

# Session encryption
SESSION_SECRET=your-session-secret-min-32-chars

# Sanity
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_READ_TOKEN=your-read-token

# Google Tag Manager (GA4 is configured inside the GTM container)
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Firebase (client-side)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Firebase Admin (server-side)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=base64-encoded-private-key

# Sentry (optional — error tracking won't load without this)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# LINE Login (direct OAuth via /api/line-login + /api/line-callback)
AUTH_LINE_ID=your-line-channel-id
AUTH_LINE_SECRET=your-line-channel-secret

# LIFF account linking (案A: LINE×Shopify 連携)
# NEXT_PUBLIC_LIFF_ID: LIFF アプリ ID（公開値。ブラウザで liff.init に使う。例 1234567890-AbCdEfGh）
# LINE_LIFF_CHANNEL_ID: LIFF が属する LINE Login チャネルの Channel ID（サーバで id_token を
#   LINE verify API に通すときの client_id。id_token の aud と一致する必要がある）。
# 前提: この LINE Login チャネルと Bot の Messaging API チャネルは「同一プロバイダー」に置くこと
#   （同一プロバイダーなら id_token の sub == Messaging userId になり、Bot/customer_linkages と揃う）。
NEXT_PUBLIC_LIFF_ID=your-liff-id
LINE_LIFF_CHANNEL_ID=your-line-login-channel-id
# LINE_LIFF_CHANNEL_SECRET: 上と同じ LINE Login チャネルの Channel Secret。
# Web マイページ発の連携 (P2 / /api/user/line-link/*) が認可コードをトークンに交換するのに使う。
# LIFF 経路 (id_token をブラウザから受け取る) では不要だったが、Web 発フローは
# サーバが自分で code→token 交換をするため必須。未設定なら連携導線は 503 で自動的に畳まれる。
# 未設定時は LINE_LOGIN_CHANNEL_SECRET にフォールバックする (LINE_LIFF_CHANNEL_ID と
# LINE_LOGIN_CHANNEL_ID は同一 Login チャネル 2009473839 を指すため両 secret は同値)。
# 本番 Vercel は LINE_LOGIN_CHANNEL_SECRET が設定済みなので、この値の新規追加は不要。
#
# ⚠ 値は「改行なし」で入れること。`vercel env add NAME production < file` のように
#   標準入力から流し込むと **末尾の改行まで値として保存される**。秘密は不透明な文字列なので
#   ダッシュボードでもログでも気づけず、症状は LINE 側の
#   `400 invalid_client / invalid client_secret` だけになる（2026-08-22 に本番の
#   LINE_LOGIN_CHANNEL_SECRET がこの状態で、Web 発の連携が全滅した）。
#   コード側は `readSecretEnvTrimmed` で trim して不感にしてあるが、入れる側でも避けること。
LINE_LIFF_CHANNEL_SECRET=your-line-login-channel-secret

# LINE の接続先ホスト（テスト専用。本番・通常運用では未設定のままにすること）
#
# 用途: LINE ログインを含む経路の自動テストを、ローカルの偽 LINE サーバーへ向けるため。
#   token 交換 / profile 取得 / verify / push は route handler からのサーバ間通信なので、
#   ブラウザ側の仕掛けでは差し替えられず、env で向き先を変えるしか手段がない。
# 既定値（未設定時）は本物の LINE ホストで、組み立てられる URL は現行と完全に同一。
#   → 本番はこの 2 行を足さない限り挙動が変わらない。
# 定義は lib/line/endpoints.ts が単一の正本。読み出しは readUrlEnvTrimmed を通すので
#   末尾改行・末尾スラッシュには不感。
#
# LINE_AUTH_BASE_URL: 認可ホスト（既定 https://access.line.me）。
#   /oauth2/v2.1/authorize、連携ダイアログ、ID トークンの iss 期待値、および
#   /{locale}/link のオープンリダイレクト許可前置きに効く。
# LINE_API_BASE_URL: API ホスト（既定 https://api.line.me）。
#   /oauth2/v2.1/token、/v2/profile、/oauth2/v2.1/verify、Messaging push に効く。
#
# ⚠ 本番に設定しないこと。認可ホストはオープンリダイレクトの許可リストの元にもなるため、
#   信頼できないホストを入れると LINE 連携の遷移先の検証が緩む。
# LINE_AUTH_BASE_URL=http://127.0.0.1:4010
# LINE_API_BASE_URL=http://127.0.0.1:4010

# --- E2E（Ring 2）専用。本番・Preview には設定しない -------------------------
#
# 下の 2 つは e2e/playwright-line-linkage.config.ts が dev サーバーへ渡すもので、
# 手で .env に書くものではない。ここに載せているのは「こういう差し替え口がある」ことを
# 1 か所で読めるようにするため。
#
# SHOPIFY_CUSTOMER_ACCOUNT_API_URL: Customer GraphQL API の向き先
#   （既定 https://shopify.com/<shop_id>/account/customer/api/<version>/graphql）。
#   AUTHORIZE / TOKEN / LOGOUT と同じく差し替えられるようにしてある。これが無いと、
#   偽の Customer Account サーバーに向けたテストでもこの 1 本だけ本物へ出ていく。
# E2E_FIRESTORE_STUB=1: プロセス内の偽 Firestore を使う（instrumentation.ts が差し込む）。
#   NODE_ENV=production では差し込みが throw する（lib/firebase/admin.ts）。
# SHOPIFY_CUSTOMER_ACCOUNT_API_URL=http://127.0.0.1:4012/account/customer/api/graphql
# E2E_FIRESTORE_STUB=1

# --- 手元の Firestore の向き先 ------------------------------------------------
#
# 手元では **既定で本番 Firestore に繋がらない**。上の FIREBASE_* が揃っていても、
# 下のどれかを選ぶまで起動時に止まる（lib/firebase/firestore-target.ts）。
# 本番・Preview では NODE_ENV / VERCEL で本番と判定されるので、この節は効かない。
#
# 選び方は docs/firestore-local-emulator.md にまとめてある。手短には:
#
#   1. エミュレーター（推奨・保存される）
#        pnpm emulator:start   … 別ターミナルで立てる（Java が要る）
#        pnpm dev:emulator     … 下の 2 つを立てて next dev を起動する
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
# NEXT_PUBLIC_FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
#   エミュレーターで使う project id（既定 demo-elxea）。demo- で始まる id は
#   エミュレーターが実サービスへ出て行かないことを保証する予約形。
# FIRESTORE_EMULATOR_PROJECT_ID=demo-elxea
#
#   2. プロセス内の偽 Firestore（Java 不要・保存されない）… 上の E2E_FIRESTORE_STUB=1
#
#   3. 本当に本番へ繋ぐ（読むだけでも本番に触れる。原則使わない）
# ALLOW_PRODUCTION_FIRESTORE=1
# NEXT_PUBLIC_ALLOW_PRODUCTION_FIRESTORE=1

# Shared secret for cx-agent identity linking API (X-API-Key header)
SYNC_API_SECRET=your-sync-api-secret

# Page Registry & Content Sync (Notion → Sanity)
NOTION_PAGE_REGISTRY_DB_ID=
NOTION_PAGE_CONTENT_DB_ID=

# Notion → Sanity 記事同期 (scripts/sync-notion-to-sanity.ts)
# ローカルは .env / サーバーは GitHub Secrets。どちらも同じ名前で process.env に入る。
NOTION_API_KEY=
NOTION_CONTENT_HUB_DB_ID=
# Sanity への書き込み用トークン (旧: ~/.config/sanity/config.json の直読み)
SANITY_API_WRITE_TOKEN=
# 同期失敗時の通知先 (未設定なら Job Summary と結果ファイルのみに残る)
SLACK_WEBHOOK_URL=
