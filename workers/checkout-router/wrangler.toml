name = "elxea-checkout-router"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Account: setaka-on@elxea.com (elxea)
# Zone: elxea.com — zone_id must be set after DNS migration
# account_id = "1ffc231d9346f5e4e4110d4850b28a70"

# Route: all traffic on elxea.com except Shopify-handled paths
# Uncomment after DNS points to Shopify and zone_id is known:
# routes = [
#   { pattern = "elxea.com/*", zone_name = "elxea.com" },
#   { pattern = "www.elxea.com/*", zone_name = "elxea.com" }
# ]

# For initial testing, use workers.dev subdomain
workers_dev = true

[vars]
VERCEL_ORIGIN = "https://elxea-web-app-setaka1103s-projects.vercel.app"
